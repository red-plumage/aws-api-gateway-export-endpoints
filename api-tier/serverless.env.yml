---
service: aws-gateway-export-endpoints
# custom domain name typically matches the service name, but
# a unique name can be specified here if needed
customDomainHostname: aws-gateway-export-endpoints

default_env: &default_env
  REGION: ${self:custom.region}
  STAGE: ${self:custom.stage}

environment: &environment
